---
phase: 01-foundation-secrets-config-dual-client-bootstrap
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/telegram/bot-client.ts
  - src/telegram/user-client.ts
  - src/main.ts
  - src/scripts/login.ts
  - docs/session-rotation.md
  - README.md
requirements: [CFG-01, CFG-03, CFG-05]
autonomous: false
user_setup:
  - service: telegram-account
    why: "Rotating the leaked MTProto session (CFG-03) requires a real Telegram account to receive the login code and generate a fresh StringSession via `npm run login`"
    env_vars:
      - name: API_ID
        source: "my.telegram.org -> API development tools"
      - name: API_HASH
        source: "my.telegram.org -> API development tools"
      - name: BOT_TOKEN
        source: "@BotFather -> token for the bot account"
      - name: SESSION_KEY
        source: "output of `npm run login` after rotation — store in Doppler, never commit"
    dashboard_config:
      - task: "Terminate the OLD leaked MTProto session before deploy"
        location: "Telegram app -> Settings -> Devices/Active Sessions -> terminate the compromised session"
      - task: "Create/populate the Doppler project + config with the six env vars"
        location: "Doppler dashboard or `doppler setup` in the repo"
must_haves:
  truths:
    - "With every env var set, the process boots, verifies both clients are connected, and logs 'Both clients connected'"
    - "If the bot token is invalid the process refuses to start and the failure names the Bot API client"
    - "If the MTProto session is invalid the process refuses to start and the failure names the user client"
    - "bot.start() is fired without await so the boot sequence does not hang on the long-poll loop"
    - "Sending SIGINT or SIGTERM stops both clients and the process exits without hanging (bounded force-exit timeout)"
    - "`npm run login` runs an interactive login and prints a fresh StringSession to stdout"
    - "A runbook documents how to terminate the leaked session and generate/store a fresh one"
  artifacts:
    - path: "src/telegram/user-client.ts"
      provides: "buildUserClient(config): GramJS TelegramClient + connect/getMe/disconnect; fail-fast on invalid session"
      exports: ["buildUserClient"]
      min_lines: 20
    - path: "src/telegram/bot-client.ts"
      provides: "buildBotClient(config): grammY Bot + init/start/stop readiness wiring"
      exports: ["buildBotClient"]
      min_lines: 15
    - path: "src/main.ts"
      provides: "bootstrap() composition root + installShutdown(); dotenv dev no-op load before loadConfig"
      exports: ["bootstrap"]
      min_lines: 40
    - path: "src/scripts/login.ts"
      provides: "Standalone interactive login that prints a fresh StringSession (CFG-03/D-04)"
      contains: "StringSession"
    - path: "docs/session-rotation.md"
      provides: "Runbook: terminate leaked session, generate fresh StringSession, store in Doppler"
      contains: "Devices"
    - path: "README.md"
      provides: "Run/setup section (Doppler path + plain-.env fallback + session generation + dev/prod run)"
      contains: "doppler run"
  key_links:
    - from: "src/main.ts"
      to: "src/config/env.ts"
      via: "bootstrap() calls loadConfig() before building any client"
      pattern: "loadConfig\\("
    - from: "src/main.ts"
      to: "src/telegram/user-client.ts"
      via: "connect() then getMe() verify before declaring ready (D-01 fail-fast)"
      pattern: "buildUserClient\\("
    - from: "src/main.ts"
      to: "src/telegram/bot-client.ts"
      via: "await bot.init() readiness then unawaited bot.start()"
      pattern: "buildBotClient\\("
    - from: "src/scripts/login.ts"
      to: "telegram StringSession"
      via: "client.start({...}) interactive login then client.session.save()"
      pattern: "session\\.save\\("
---

## Phase Goal

**As an** operator of Mr. Kek, **I want to** start one process that boots both the grammY Bot API client and the GramJS user client, proves both are connected, shuts down cleanly on a signal, and gives me a repeatable way to rotate the leaked session, **so that** the bot runs as a non-hanging walking skeleton with no compromised credential live.

<objective>
Complete the walking skeleton. Build the dual-client composition root that loads config (from plan 01-01), connects and verifies BOTH Telegram clients, logs "Both clients connected", fires the long-poll loop without blocking boot, and installs a graceful SIGINT/SIGTERM shutdown with a force-exit timeout so the process can never hang. Ship the session-rotation slice: an executable login helper that prints a fresh StringSession, a rotation runbook, the README run/setup section, and the Doppler/plain-.env onboarding path.

Purpose: Deliver Success Criteria 1, 3, and 5 — both clients boot or the process refuses to start naming the failed client (D-01), the leaked session is rotatable via a repeatable helper + runbook (CFG-03/D-04), and shutdown is bounded (D-05). After this plan a real operator can run the bot end-to-end.

Output: src/telegram/user-client.ts, src/telegram/bot-client.ts, src/main.ts (bootstrap + installShutdown), src/scripts/login.ts, docs/session-rotation.md, README.md run/setup section.
</objective>

<execution_context>
@/home/ilia/IdeaProjects/mrkekbot/.claude/gsd-core/workflows/execute-plan.md
@/home/ilia/IdeaProjects/mrkekbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@SPEC.md
@.planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-CONTEXT.md
@.planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-RESEARCH.md
@.planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Telegram client adapters — GramJS user client + grammY bot client (D-01 fail-fast)</name>
  <files>src/telegram/user-client.ts, src/telegram/bot-client.ts</files>
  <read_first>
    - .planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-RESEARCH.md (§Code Examples: GramJS reconnect-from-env+verify — new TelegramClient(new StringSession(SESSION_KEY), API_ID, API_HASH, {connectionRetries}), connect() then getMe() verify, disconnect() on shutdown; §Pattern 1 grammY bot.init()/start()/stop(); §Pitfall 2 connect() is not proof of a valid session — must getMe(); §Pitfall 5 GramJS deep import telegram/sessions/index.js under NodeNext)
    - src/config/env.ts (the Config type produced by plan 01-01 — fields BOT_TOKEN, API_ID, API_HASH, SESSION_KEY, MAIN_CHAT_ID, RELAY_CHANNEL_ID)
    - .planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-CONTEXT.md (D-01 fail-fast on either client; user client is booted+verified ONLY this phase — do NOT wire any feature, no casino, no message handlers)
    - .claude/CLAUDE.md (grammY ^1.44, telegram/GramJS ^2.26 StringSession)
  </read_first>
  <action>
    Create src/telegram/user-client.ts exporting buildUserClient(config: Config) that constructs a GramJS TelegramClient with new StringSession(config.SESSION_KEY), config.API_ID, config.API_HASH and { connectionRetries: 5 }, importing StringSession via the explicit subpath telegram/sessions/index.js to survive NodeNext resolution (Pitfall 5). Expose the constructed client plus a way to connect-and-verify: connect() then getMe() (connect() alone is NOT proof of a valid session — Pitfall 2/D-01) and a disconnect() for shutdown. The verify step must reject/throw if getMe() fails so the composition root can refuse to start and name the user client. Do NOT register any event handlers, do NOT call any raw API (messages.Search etc are v2/out of scope) — boot-and-verify only. Create src/telegram/bot-client.ts exporting buildBotClient(config: Config) that constructs a grammY Bot with config.BOT_TOKEN and exposes the bot so the root can await bot.init() (readiness signal — init calls getMe and sets botInfo) and later call bot.start({ onStart }) unawaited and bot.stop() on shutdown. Do NOT await bot.start() inside this module and do NOT register message/command handlers (Phase 3). Keep both adapter surfaces tiny (construct + the lifecycle calls) so a future mtcute migration stays contained.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - src/telegram/user-client.ts exports buildUserClient and constructs TelegramClient with StringSession from config.SESSION_KEY (grep returns buildUserClient and StringSession)
    - src/telegram/user-client.ts imports StringSession from "telegram/sessions/index.js" (explicit NodeNext subpath) — grep returns that import path
    - The user-client verify path calls getMe() after connect() (grep returns getMe) and surfaces a rejection on failure (not swallowed)
    - src/telegram/bot-client.ts exports buildBotClient and constructs a grammY Bot with config.BOT_TOKEN (grep returns buildBotClient and Bot)
    - Neither adapter registers handlers nor calls bot.start() with await: grep src/telegram/ for "await bot.start" returns nothing; grep for "bot.on(" / "client.addEventHandler" returns nothing
    - `npx tsc --noEmit` passes (type-checks clean under NodeNext strict)
  </acceptance_criteria>
  <done>Two minimal adapters: buildUserClient (GramJS, connect+getMe verify, disconnect, fail-fast on invalid session per D-01) and buildBotClient (grammY, init readiness + unawaited start + stop), with no feature wiring and a clean tsc.</done>
</task>

<task type="auto">
  <name>Task 2: Composition root — non-blocking dual-client bootstrap + graceful force-exit shutdown (CFG-05, D-01, D-05, D-08)</name>
  <files>src/main.ts</files>
  <read_first>
    - .planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-RESEARCH.md (§Pattern 1 bootstrap ordering: loadConfig -> user connect+getMe -> bot.init -> log both connected -> bot.start unawaited with .catch -> installShutdown; §Pattern 3 graceful shutdown with setTimeout(...).unref() force-exit; §Pitfall 1 bot.start() never resolves — never await it; §Pitfall 7 dotenv import before loadConfig in dev, no-op under Doppler; §Open Question 2 force-exit 8s recommendation)
    - src/config/env.ts and src/logger.ts (from plan 01-01: loadConfig(), createLogger())
    - src/telegram/user-client.ts and src/telegram/bot-client.ts (Task 1 of this plan: buildUserClient, buildBotClient)
    - .planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-CONTEXT.md (D-01 fail-fast either client + name which failed; D-05 graceful shutdown with force-exit timeout ~5-10s; D-08 dotenv as local fallback before loadConfig; Claude's Discretion: shutdown ordering + timeout duration)
  </read_first>
  <action>
    Create src/main.ts as the composition-root entrypoint that the dev/build/start npm scripts target. At the very top, before anything else, load the plain-.env dev fallback (import "dotenv/config" or equivalent) so a contributor without Doppler still gets process.env populated; under doppler run this is a harmless no-op (Pitfall 7/D-08). Then define and run bootstrap(): (1) call loadConfig() from src/config/env.ts (it exits non-zero on a bad env before any client is built); (2) build the logger via createLogger(); (3) build the user client, connect() and verify with getMe() — on failure log a fatal that names the GramJS user client and exit non-zero (D-01); (4) build the bot client and await bot.init() — on failure log a fatal that names the Bot API client and exit non-zero (D-01); (5) log the exact message "Both clients connected"; (6) fire bot.start({ onStart }) WITHOUT await (it never resolves while polling — Pitfall 1) and attach a .catch that logs fatal and exits if polling crashes; (7) call installShutdown. Implement installShutdown(bot, user, log): register process.once handlers for SIGINT and SIGTERM that are idempotent (ignore a repeat signal), start a force-exit setTimeout of ~8s with .unref() that calls process.exit(1) if cleanup hangs (D-05), then await bot.stop() then user.disconnect(), log "clean shutdown complete", and process.exit(0); on any error during cleanup, log and process.exit(1). Order: connect the user client BEFORE firing the unawaited bot.start(). Do NOT await bot.start() anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/main.ts','utf8'); if(/await\s+bot\.start/.test(s)){console.error('bot.start must NOT be awaited');process.exit(1);} if(!/Both clients connected/.test(s)){console.error('missing readiness log');process.exit(1);} if(!/SIGINT/.test(s)||!/SIGTERM/.test(s)){console.error('missing signal handlers');process.exit(1);} if(!/unref\(\)/.test(s)){console.error('missing force-exit unref timer');process.exit(1);} console.log('main.ts structure OK');"</automated>
  </verify>
  <acceptance_criteria>
    - src/main.ts loads dotenv before loadConfig (grep shows a dotenv import positioned before the loadConfig call)
    - bootstrap() calls loadConfig(), buildUserClient, buildBotClient in that order and logs the exact string "Both clients connected" (the node assertion checks the string is present)
    - bot.start() is never awaited: the node assertion fails the task if /await\s+bot\.start/ matches src/main.ts
    - On user-client getMe() failure the fatal log names the user/GramJS client; on bot.init() failure it names the Bot API client (grep src/main.ts shows both client names in the respective error branches)
    - installShutdown registers process.once for SIGINT and SIGTERM and a setTimeout(...).unref() force-exit (node assertion checks SIGINT, SIGTERM, unref())
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>src/main.ts boots both clients with verified readiness, logs "Both clients connected", fires bot.start() unawaited (no hang — Pitfall 1), fail-fasts naming the failed client (D-01), and installs an idempotent SIGINT/SIGTERM shutdown with an 8s unref'd force-exit (D-05). tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Session-rotation slice — login helper, runbook, README setup (CFG-03, CFG-01, D-04, D-07, D-08)</name>
  <files>src/scripts/login.ts, docs/session-rotation.md, README.md</files>
  <read_first>
    - .planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-RESEARCH.md (§Pattern 4 login helper: new TelegramClient(new StringSession(""), apiId, apiHash, {connectionRetries:5}), client.start({phoneNumber, password, phoneCode, onError}), console.log(client.session.save()), disconnect; §Open Question 1 prefer node:readline/promises over input; §Runtime State Inventory + Pitfall 3 the leaked session must be terminated in Telegram, not just moved to env; §Common Pitfalls / Session Rotation Runbook steps; D-08 doppler run wrapping + plain-.env fallback)
    - SPEC.md §3 / §11.1 (the hardcoded-session security bug being fixed) and §4 (API_ID/API_HASH/SESSION_KEY env vars)
    - .planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-CONTEXT.md (D-04 executable login helper + runbook, repeatable, not docs-only; D-07 README run/setup section + the npm scripts; D-08 Doppler primary, plain-.env fallback, .env.example as key schema)
    - package.json (the login / login:local scripts defined in plan 01-01 that invoke this helper)
    - .env.example (the key list this README documents)
  </read_first>
  <action>
    Create src/scripts/login.ts as a standalone tsx entrypoint (NOT imported by the running bot) that performs an interactive GramJS login and prints a fresh StringSession. Read API_ID and API_HASH from process.env (the helper runs under doppler run -- or plain .env). Construct new TelegramClient(new StringSession(""), Number(API_ID), API_HASH, { connectionRetries: 5 }), call client.start({ phoneNumber, password, phoneCode, onError }) where the prompts are async functions reading from the terminal — prefer node:readline/promises (zero dependency, RESEARCH Open Question 1) for phone number, login code, and optional 2FA password. After login, console.log the result of client.session.save() with a clear label telling the operator to store it as SESSION_KEY in Doppler and NEVER commit it, then disconnect and exit. Do NOT write the session to any file and do NOT echo it into any committed artifact. Create docs/session-rotation.md as the rotation runbook (CFG-03): (1) terminate the OLD leaked session in Telegram -> Settings -> Devices/Active Sessions (moving to env does NOT invalidate the leaked string — Pitfall 3), (2) ensure API_ID/API_HASH are set, (3) run `npm run login` (or `doppler run -- npm run login`), (4) copy the printed StringSession into Doppler (and/or local .env) as SESSION_KEY, (5) confirm the bot boots with the new session, (6) note the launch gate — the leaked session must be rotated before any deploy (STATE.md blocker). Add a README.md run/setup section (D-07): prerequisites (Node 22, npm), install, the Doppler path (install CLI, doppler login, doppler setup, run via doppler run -- npm run dev / start) AND the plain-.env fallback (copy .env.example to .env, fill values, use the :local scripts), how to generate a session (link to the runbook), and the dev/build/start/login npm scripts. README and runbook must reference env var names only — never a real secret value.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l=fs.readFileSync('src/scripts/login.ts','utf8'); if(!/session\.save\(/.test(l)){console.error('login helper must print client.session.save()');process.exit(1);} if(!/StringSession/.test(l)){process.exit(1);} const rb=fs.readFileSync('docs/session-rotation.md','utf8'); if(!/Devices|Active Sessions/i.test(rb)){console.error('runbook must cover terminating the leaked session');process.exit(1);} const rm=fs.readFileSync('README.md','utf8'); if(!/doppler run/.test(rm)){console.error('README must document the doppler run path');process.exit(1);} if(!/\.env\.example/.test(rm)){console.error('README must document the plain-.env fallback');process.exit(1);} console.log('rotation slice OK');"</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/login.ts constructs a TelegramClient with an empty StringSession and prints client.session.save() (grep returns StringSession and session.save)
    - The helper reads prompts via node:readline/promises (or, if input was chosen in 01-01, that devDependency) and reads API_ID/API_HASH from process.env (no hardcoded credential)
    - The helper writes the session to stdout only — it never writes it to a tracked file (grep src/scripts/login.ts shows console.log of the session, no fs.write of the session)
    - docs/session-rotation.md covers terminating the leaked session (mentions Devices / Active Sessions), generating via `npm run login`, and storing as SESSION_KEY (the node assertion checks for Devices/Active Sessions)
    - README.md documents BOTH the `doppler run` path and the plain-.env fallback referencing .env.example (the node assertion checks for "doppler run" and ".env.example")
    - No real secret or the leaked session string appears in src/scripts/login.ts, docs/session-rotation.md, or README.md (grep -rI for a session-string literal returns nothing)
  </acceptance_criteria>
  <done>A repeatable `npm run login` prints a fresh StringSession to stdout (CFG-03/D-04), docs/session-rotation.md is the terminate-then-regenerate runbook, and README documents the Doppler + plain-.env onboarding and all npm scripts (D-07/D-08). No secret in any artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Rotate the leaked session and verify the walking skeleton boots + shuts down</name>
  <action>STOP for human verification — this is a blocking-human checkpoint, not autonomous work. The executor pauses here. The human must (1) terminate the leaked MTProto session in Telegram, (2) generate a fresh StringSession via `npm run login`, (3) boot the skeleton and confirm both clients connect + the missing-var refusal works + SIGINT/SIGTERM exit cleanly, following the steps in how-to-verify. Resume only on the resume-signal. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>The complete walking skeleton: a single process that boots both Telegram clients with verified readiness, logs "Both clients connected", shuts down cleanly on signal, plus the `npm run login` session-rotation helper and runbook. This checkpoint exists because the leaked MTProto session is a live account-takeover credential (CFG-03 launch gate, STATE.md blocker) and only a human with the Telegram account can terminate it and generate a fresh one.</what-built>
  <how-to-verify>
    1. Ensure Node 22 is installed (`node --version` reports v22.x) and run `npm install`.
    2. ROTATE THE LEAKED SESSION FIRST: open Telegram -> Settings -> Devices/Active Sessions and terminate the old compromised session (do this even before generating the new one).
    3. With API_ID/API_HASH set (via Doppler or .env), run `npm run login` (or `npm run login:local`). Complete the phone/code/2FA prompts. Confirm it prints a fresh StringSession to the terminal. Store it as SESSION_KEY (Doppler and/or .env). Confirm the value was NOT written to any file in the repo and is not staged in git.
    4. With all six env vars set (BOT_TOKEN, API_ID, API_HASH, SESSION_KEY, MAIN_CHAT_ID, RELAY_CHANNEL_ID), run `npm run dev` (or `npm run dev:local`). Expected: logs show the user client connected, the bot connected, then exactly "Both clients connected", then the long-poll start — and the process does NOT hang at boot.
    5. Negative check: unset BOT_TOKEN and run again. Expected: the process refuses to start, names the missing var (and/or the Bot API client), and exits non-zero.
    6. Send SIGINT (Ctrl-C) to the running process. Expected: it logs a clean shutdown and the process exits within ~8s without hanging; SIGTERM behaves the same.
  </how-to-verify>
  <resume-signal>Type "approved" once the leaked session is terminated + rotated, both clients log connected, missing-var refusal works, and SIGINT/SIGTERM exit cleanly — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal -> login helper | Phone/code/2FA and the generated StringSession transit the terminal; the session must reach Doppler/.env only, never a file or git |
| Telegram account -> live session | The leaked StringSession remains a full-account credential until terminated server-side in Telegram |
| process -> Telegram (Bot API + MTProto) | Both clients authenticate with bearer credentials; a half-valid config must not silently connect to the wrong account/chat |
| process -> logs | Boot/shutdown logs must not leak token or session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-SR | Spoofing / Elevation | leaked MTProto StringSession (live runtime state) | mitigate | Blocking human checkpoint: terminate the old session in Telegram -> Devices AND regenerate via `npm run login`; runbook documents the terminate-then-regenerate order (CFG-03, Pitfall 3) — moving to env alone does NOT invalidate the leaked string |
| T-01.2-01 | Information Disclosure | src/scripts/login.ts | mitigate | Session printed to stdout only; never written to a tracked file; operator instructed to store in Doppler and never commit; .gitignore (01-01) blocks .env |
| T-01.2-02 | Tampering | bootstrap() readiness checks | mitigate | D-01 fail-fast: getMe() (user) + bot.init() (bot) verify real authorization before "Both clients connected"; refuses to start naming the failed client (Pitfall 2 — connect() alone is not proof) |
| T-01.2-03 | Denial of Service | shutdown handler | mitigate | Bounded setTimeout(...).unref() force-exit (~8s) guarantees the process can never hang on SIGINT/SIGTERM (D-05) |
| T-01.2-04 | Information Disclosure | boot/shutdown logging | mitigate | Use the redacting logger from 01-01 (createLogger); log botInfo.username/userId, never the raw token/session/config object |
| T-01.2-SC | Tampering | npm installs (grammy/telegram) | accept | Cleared in RESEARCH Package Legitimacy Audit (telegram stale-but-proven; grammy SUS false positive). No blocking package checkpoint required |
</threat_model>

<verification>
- `npx tsc --noEmit` passes across all of src/ (adapters + main + login helper, NodeNext strict, explicit .js extensions)
- Static structure checks: bot.start() is never awaited; "Both clients connected" is logged; SIGINT/SIGTERM handlers + unref'd force-exit present
- login helper prints client.session.save() and writes nothing to a tracked file
- runbook covers terminate-then-regenerate; README documents Doppler + plain-.env paths
- Human checkpoint confirms Success Criteria 1, 3, 5 live: both clients connect, missing-var refusal names the gap, leaked session rotated, SIGINT/SIGTERM exit clean
</verification>

<success_criteria>
- The process boots and logs "Both clients connected"; a missing secret/id refuses start and names what is missing (Success Criterion 1, CFG-05)
- The compromised MTProto session is rotated and a runbook documents fresh-StringSession generation (Success Criterion 3, CFG-03)
- SIGINT/SIGTERM stop both clients cleanly with no hung process (Success Criterion 5, CFG-05, D-05)
- No secret appears in source — clients/login read only from env (CFG-01)
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

| Symbol / file | Kind | Detail |
|---|---|---|
| src/telegram/user-client.ts | module | `buildUserClient(config)` — GramJS TelegramClient + connect/getMe verify + disconnect (fail-fast on invalid session) |
| src/telegram/bot-client.ts | module | `buildBotClient(config)` — grammY Bot + init readiness + unawaited start + stop |
| src/main.ts | module (entrypoint) | `bootstrap()` composition root + `installShutdown()`; dotenv dev no-op load before loadConfig; logs "Both clients connected"; 8s unref force-exit |
| src/scripts/login.ts | script (entrypoint) | interactive GramJS login printing a fresh StringSession to stdout (CFG-03/D-04) |
| docs/session-rotation.md | doc | rotation runbook (terminate leaked session -> regenerate -> store as SESSION_KEY) |
| README.md | doc | run/setup section: Doppler path + plain-.env fallback + session generation + dev/build/start/login scripts |
| "Both clients connected" | log contract | the readiness string Success Criterion 1 asserts |
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation-secrets-config-dual-client-bootstrap/01-02-SUMMARY.md` when done
</output>